int cheat = 0;
        foreach (Process process in Process.GetProcessesByName("CheatEngine72"))
        {
            counter++;
        }
	if(cheat > 0)
          {
             //get code for closing Poltergeist process
          }
